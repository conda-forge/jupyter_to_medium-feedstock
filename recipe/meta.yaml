{% set name = "jupyter_to_medium" %}
{% set version = "0.2.7" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('_', '-') }}-{{ version }}.tar.gz
  sha256: acb12c5fe98a38d9a14988bc7905dd1326b52c716c3e0a7681f9b69350e4d235

build:
  number: 0
  noarch: python
  entry_points:
    - jupyter_to_medium=jupyter_to_medium._command_line:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - beautifulsoup4
    - matplotlib-base >=3.1
    - nbconvert
    - numpy
    - python >=3.6
    - requests

test:
  imports:
    - jupyter_to_medium
  commands:
    - pip check
    - jupyter_to_medium --help
  requires:
    - pip

about:
  home: https://dexplo.org/jupyter_to_medium
  summary: Publish a Jupyter Notebook as a Medium blogpost
  license: MIT
  license_file: LICENSE
  doc_url: https://dexplo.org/jupyter_to_medium
  dev_url: https://github.com/dexplo/jupyter_to_medium

extra:
  recipe-maintainers:
    - tdpetrou
